FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package.json package-lock.json ./

# Install ALL dependencies (including dev)
RUN npm install

# Copy source code
COPY tsconfig.json ./
COPY src ./src

# Expose port
EXPOSE 8080

# Start with hot-reloading
CMD ["npm", "run", "dev"]
